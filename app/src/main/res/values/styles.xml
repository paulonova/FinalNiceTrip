<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Changing the Preference background color... -->
    <style name="PreferenceTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:background">#fe000000</item>
        <item name="android:textColor">@color/orange_color</item>
    </style>

</resources>
